language: node_js

node_js:
  - stable

sudo: false

install:
- npm install

#script:
#- vuepress check-md

before_deploy:
- npm run build

deploy:
  provider: pages
  skip-cleanup: false
  github-token: $GITHUB_TOKEN
  local-dir: .vuepress/dist
  keep-history: true
  committer-from-gh: true
  on:
    branch: master